import { Layout } from '@/layout';
import { Apartments } from '@/widgets/apartments';

export const Catalog = () => {
    return (
        <Layout>
            <Apartments />
        </Layout>
    );
};
