import { Layout } from '@/layout';
import { Apartments } from '@/widgets/apartments';

export const Main = () => {
    return (
        <Layout>
            <Apartments />
        </Layout>
    );
};
